// Placeholder page to satisfy generated route type checks for documents under a record.
// If you later build a real documents view, replace this stub.
import { notFound } from "next/navigation";

export default function RecordDocumentsPage() {
  notFound();
}
